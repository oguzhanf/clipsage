<Window x:Class="ClipSage.App.UpdateCheckDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClipSage.App"
        mc:Ignorable="d"
        Title="clipsage / check for updates"
        MinHeight="420" MinWidth="600"
        Width="680" Height="480"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Style="{StaticResource ZuneWindow}">

    <Grid Margin="48,32,48,32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" MinHeight="200"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   FontFamily="Segoe UI Light" FontWeight="Light"
                   FontSize="56"
                   Foreground="{StaticResource ZuneForeground}"
                   Text="check for updates"
                   Margin="0,0,-200,20"/>

        <ProgressBar x:Name="ProgressBar" Grid.Row="1"
                     Style="{StaticResource ZuneProgressBar}"
                     IsIndeterminate="True"
                     Margin="0,0,0,20"/>

        <Border Grid.Row="2"
                BorderBrush="{StaticResource ZuneLine}"
                BorderThickness="0,1,0,1"
                Background="{StaticResource ZuneSurface1}"
                Margin="0,0,0,20">
            <ScrollViewer x:Name="LogScrollViewer" VerticalScrollBarVisibility="Auto">
                <TextBox x:Name="LogTextBox"
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         Background="Transparent"
                         BorderThickness="0"
                         Foreground="{StaticResource ZuneForeground2}"
                         FontFamily="Consolas"
                         FontSize="12"
                         Padding="16"/>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="CloseButton" Grid.Column="1"
                    Style="{StaticResource ZuneButton}"
                    Content="close"
                    Padding="22,10"
                    Margin="0,0,12,0"
                    Click="CloseButton_Click"/>
            <Button x:Name="DownloadButton" Grid.Column="2"
                    Style="{StaticResource ZuneButtonPrimary}"
                    Content="download"
                    Padding="22,10"
                    IsEnabled="False"
                    Click="DownloadButton_Click"/>
        </Grid>
    </Grid>
</Window>
